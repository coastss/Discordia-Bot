--// Initialize every new command file with these variables, otherwise the command will not run. The "Discordia" variable is needed depending on what you are doing.
--// Optional: local Discordia = _G.Discordia
local Client = _G.Client
local Message = _G.Message
local Arguments = _G.Arguments

return(Message:reply("pong!"))
